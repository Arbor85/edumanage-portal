# Improve PlanFormModal.tsx
Dialog should take full view height and width
process of edditing routine should be horizontal wizard
on top user can see field to add/edit name of plan (always visible)

scheduled workouts should be visible on left (vertical) wizard steps allowin user to skip between them. at the end there is (add new button in form of step)
switch between steps should be animated
present only one excercise at onece on single step


# Technical
- indroduce new shared busines component that opens regular dialog and allow search of Clients and Workouts (search should not be done via drop down but via list with search). 